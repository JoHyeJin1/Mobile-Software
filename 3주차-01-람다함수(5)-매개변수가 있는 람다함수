package ddwu.mobile.week02.kotlintest

fun main(){
    val lambdaPower : (Int) -> Int = { num : Int ->
        num * num
    }

    println(lambdaPower(2))
    
    val lambdaWithIt : (Int) -> Int = {
        it * it
    }
    
    println(lambdaWithIt(3))
}

/*
람다함수의 매개변수가 하나일 경우 생략 후 키워드 it으로 사용 가능
이 때, (Int) -> Int 생략 불가
*/
